[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "stdenv.cc.cc.lib", "zlib", "libGL", "glib"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python tgbot.py"